# Read Java version from .java-version file (single source of truth)
JAVA_VERSION=$(cat .java-version)
export JAVA_HOME=$(/usr/libexec/java_home -v ${JAVA_VERSION})
export PATH="$JAVA_HOME/bin:$PATH"

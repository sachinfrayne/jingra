FROM demo-metrics
| WHERE `system.network.io` IS NOT NULL AND device == "eth0"
| STATS total_bytes = sum(`system.network.io`),
        avg_bytes = avg(`system.network.io`)
  BY `host.name`, direction
| SORT total_bytes DESC
| LIMIT {{size}}
